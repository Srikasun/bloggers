name: inkhaven
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  firebase_auth: ^5.1.4
  provider: ^6.1.2
  cloud_firestore: ^5.2.1
  firebase_analytics: ^11.4.4
  firebase_dynamic_links: ^6.1.4
  share_plus: ^10.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: true
  ios: true
  image_path: "assets/inkhavenlogo.png"

flutter:
  uses-material-design: true
  assets:
    - assets/inkhavenlogo.png
